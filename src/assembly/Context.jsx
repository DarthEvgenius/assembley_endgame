import { createContext } from "react";

export const LanguagesContext = createContext(null);

export const WordContext = createContext('react');

export const SelectedCharsContext = createContext(null);

export const AttemptsLeftContext = createContext(null);

